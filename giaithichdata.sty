Danh sách các thuộc tính bạn đã cung cấp là các thuộc tính được sử dụng trong tập dữ liệu để phân loại nấm. Dưới đây là mô tả ngắn gọn về mỗi thuộc tính:

cap-shape: Hình dạng của mũ nấm (có các giá trị: b = bell, c = conical, x = convex, f = flat, k = knobbed, s = sunken).

cap-surface: Bề mặt của mũ nấm (có các giá trị: f = fibrous, g = grooves, y = scaly, s = smooth).

cap-color: Màu sắc của mũ nấm (có các giá trị: n = brown, b = buff, c = cinnamon, g = gray, r = green, p = pink, u = purple, e = red, w = white, y = yellow).

bruises: Sự xuất hiện của vết bầm trên thân nấm (có các giá trị: t = có vết bầm, f = không có vết bầm).

odor: Mùi của nấm (có các giá trị: a = hôi, l = hương thơm, c = hương cam, y = hương dễ chịu, f = hương mùi của cá, m = hương béo, n = không mùi, p = hương cay, s = hương mùi như mùi khét, y = hương mùi như mùi xì dầu).

gill-attachment: Sự kết nối của màng gill với thân nấm (có các giá trị: a = gắn chặt, d = giảm, f = tự do, n = không có thông tin).

gill-spacing: Khoảng cách giữa các màng gill (có các giá trị: c = gần nhau, w = rộng).

gill-size: Kích thước của màng gill (có các giá trị: b = rộng, n = hẹp).

gill-color: Màu sắc của màng gill (có các giá trị: k = đen, n = nâu, b = buff, h = chocolate, g = xám, r = xanh lá cây, o = da cam, p = hồng, u = tím, e = đỏ, w = trắng, y = vàng).

stalk-shape: Hình dạng của thân nấm (có các giá trị: e = mở rộng, t = thuôn).

stalk-root: Rễ của thân nấm (có các giá trị: b = nút, c = gai, u = không có, e = mở rộng, z = chùm, r = gốc, ? = không rõ).

stalk-surface-above-ring: Bề mặt của thân nấm ở phía trên vòng (có các giá trị: f = sợi, y = lõi, k = mịn, s = nhám).

stalk-surface-below-ring: Bề mặt của thân nấm ở phía dưới vòng (có các giá trị: f = sợi, y = lõi, k = mịn, s = nhám).

stalk-color-above-ring: Màu sắc của thân nấm ở phía trên vòng (có các giá trị: n = brown, b = buff, c = cinnamon, g = gray, o = da cam, p = hồng, e = đỏ, w = trắng, y = vàng).

stalk-color-below-ring: Màu sắc của thân nấm ở phía dưới vòng (có các giá trị: n = brown, b = buff, c = cinnamon, g = gray, o = da cam, p = hồng, e = đỏ, w = trắng, y = vàng).

veilTiếp tục từ phần trước:

veil-color: Màu sắc của màng che (có các giá trị: n = brown, o = da cam, w = trắng, y = vàng).

ring-number: Số lượng vòng trên thân nấm (có các giá trị: n = không có, o = một, t = hai).

ring-type: Loại vòng trên thân nấm (có các giá trị: c = mũ, e = mở rộng, f = có màng, l = lõm, n = không có vòng, p = nắp).

spore-print-color: Màu sắc của bụi phấn (có các giá trị: k = đen, n = nâu, b = buff, h = chocolate, r = xanh lá cây, o = da cam, u = tím, w = trắng, y = vàng).

population: Dân số (có các giá trị: a = nhiều, c = đông, n = hiếm, s = một, v = có, y = giữa).

habitat: Môi trường sống (có các giá trị: g = cánh đồng, l = lá rừng, m = đồng cỏ, p = môi trường đô thị, u = vùng không có lá cây, w = rừng, d = vùng đáy núi).

Những thuộc tính này được sử dụng để mô tả các đặc trưng của một loại nấm và được sử dụng trong quá trình phân loại. Mỗi thuộc tính có thể có một tập hợp các giá trị khác nhau, và thông qua việc phân tích các thuộc tính này trên tập dữ liệu, một mô hình có thể được huấn luyện để dự đoán loại nấm dựa trên các đặc trưng đó.

